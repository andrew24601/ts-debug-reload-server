{
  "name": "ts-debug-reload-server",
  "version": "0.0.25",
  "description": "TypeScript debug hot reload server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Barry",
  "license": "ISC",
  "bin": {
    "ts-debug-reload-server": "./bin/ts-debug-reload-server.js"
  },
  "dependencies": {
    "json5": "^2.1.0",
    "lines-and-columns": "^1.1.6",
    "websocket": "^1.0.28"
  }
}
